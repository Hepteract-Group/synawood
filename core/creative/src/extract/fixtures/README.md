# Extract fixtures

| File | Use |
|---|---|
| `sample.pdf` | Real multi-page PDF for `pdf-adapter` integration (pdf-parse test corpus) |
| `sample-url.html` | HTML digest fixture for URL adapter / SSRF suite |

Brief JSON fixtures live under `src/brief/fixtures/` (#160).
