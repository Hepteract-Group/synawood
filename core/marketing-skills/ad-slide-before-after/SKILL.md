---
name: ad-slide-before-after
description: Pair a messy-before slide with a clean-after slide. Same camera language so the contrast is the ad.
category: slides
locked: true
---

# Before / after

Brand (non-negotiable): use this project's Brand Studio — logo, primary/accent, fonts, CTA, claims. Do not invent a palette. Do not fake product UI. Path C / set_slide carries headlines; generated pixels are scenes, not typography.

## Rules

Two slides, same framing idea, opposite state.

- Do not morph in one image. Two generate_slide_background calls.
- After uses product stills when they exist; never fake the UI.
- Keep type parallel so the eye reads the delta.

## Tools

Use Studio Tools only. No shell, no vendor CLIs, no skills.sh scripts.
