---
name: ad-slide-brand-lockup
description: Logo bug every slide via Path C. Do not hide the brand until the end unless the hook needs a cold open.
category: slides
locked: true
---

# Brand lockup

Brand (non-negotiable): use this project's Brand Studio — logo, primary/accent, fonts, CTA, claims. Do not invent a palette. Do not fake product UI. Path C / set_slide carries headlines; generated pixels are scenes, not typography.

## Rules

import_product_brand / Brand Studio first.

- generate_* requires project.brand.
- Logo in the image pixels is a last resort; Remotion bug is the default.
- CTA slide can go heavier on brand fill.

## Tools

Use Studio Tools only. No shell, no vendor CLIs, no skills.sh scripts.
