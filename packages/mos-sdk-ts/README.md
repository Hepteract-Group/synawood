# mos-sdk-ts

Optional typed stub for Synawood `/api/v1`. Private workspace package. **Do not publish to npm.**

Covers Product API key (`Authorization: Bearer`) and `GET /api/v1/health`. Other routes stay out of this stub until endpoint parity is complete (ADR-0038). Does not wrap inbound MCP tools.
