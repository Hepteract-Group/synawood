import { NotFoundView } from '@/components/not-found/NotFoundView'

/** Keeps the Synawood shell when a (mos) segment triggers notFound(). */
export default function MosNotFound() {
  return <NotFoundView />
}
